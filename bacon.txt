WITH RECURSIVE above_here(x) as (select parent_id from snmpdata where id = 98231
 union all
 select parent_id from snmpdata, above_here
 where snmpdata.id = above_here.x)
 select id, parent_id, oid, oid_name from snmpdata where id in above_here;
